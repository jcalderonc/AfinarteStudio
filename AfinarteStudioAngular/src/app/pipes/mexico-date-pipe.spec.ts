import { MexicoDatePipe } from './mexico-date-pipe';

describe('MexicoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MexicoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
