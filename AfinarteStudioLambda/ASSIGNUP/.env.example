# ASSIGNUP Lambda Environment Variables
# Copy this file to .env for local development

# Environment variables for ASSIGNUP Lambda Function
# Copy this file to .env and fill in your actual values

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
DB_NAME=afinarteStudio

# JWT Configuration (if needed for future token generation)
JWT_SECRET=tu-clave-secreta-super-segura-aqui
JWT_EXPIRES_IN=24h

# For local development
NODE_ENV=development

# AWS Configuration (if needed)
AWS_REGION=us-east-1

# Optional: Override default validation rules
MIN_PASSWORD_LENGTH=4
REQUIRE_EMAIL_VERIFICATION=false
